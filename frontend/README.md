# Frontend

This folder is reserved for frontend-specific code and configuration.

The current Next.js application still lives at the project root under `src/`.
You can incrementally move frontend code into this directory as the project evolves.


